# 真宿主联测的运行产物,不入库。
#
# 两个理由,第二个是硬的:
#   1. 它们是每次跑出来的结果,不是源码,重跑即得。
#   2. 它们**必然含个人绝对路径**。实测本轮一次运行的产物里有 43 处 `/Users/<name>/…`
#      分布在 host.log(30)、results.json(9)、probe-host.log(2)、run.log(1)、
#      probe-host-findings.json(1) —— 宿主与 playwright 都会把绝对路径打进日志,
#      截图也可能带上工作树路径。这不是能靠"记得脱敏"解决的,所以从入库层挡掉。
#
# 需要交付证据时,把脱敏后的副本放进 docs/engineering/<轮次>/logs/ 并在文件头声明
# 脱敏范围(本轮 frontend-test.txt 就是这么做的),不要直接提交这里的原始产物。
artifacts/
