deepseek-harness @ a66e470204 (0.1.2-rc.1) — vendored 2026-09-04

Membrane realigned to the 0.1.2 transport (batch B of TASK-2026-09-04-001):
- 2 envelopes (client-request/server-response); errors {code,message,details} open codes
- unary payload = {args:{<paramName>:value}} (param-name keyed; see api-client ARG_KEY)
- single /api/remote.mux WebSocket multiplexes logical streams (session/follow,
  workspace/follow, $events); Host→Client asks answered via $events/result
- session.history→session/page+follow; session.models→session/modelCatalog;
  host.describe removed (home via $events ready); workspace.list→workspace/follow
- membrane leaned to the ~14 methods AgOS calls; settings editor + llm/credentials
  directory deferred to batch B2 (SettingsPage degraded to open-document only)
Verified: typecheck+build+replay(392 sessions)+287 tests green; unary+mux wire
smoke against a real 0.1.2 sandbox gateway (zero model calls).
