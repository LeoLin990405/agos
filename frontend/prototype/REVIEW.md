# 原型审查记录(Claude,2026-08-20)

**结论:视觉方向 PASS,远超插件层旧版;深色(默认)三屏全过;浅色一轮补丁后可定稿。**

## 已由审查直接修
- 终端卡(.terminal-card)双主题恒深底 `#090c13`:浅色下 `--code-bg:#f8fafc` 会把硬编码浅色输出文字洗掉;终端保持深底也是更对的设计。

## 退回迭代(浅色一轮补丁,按页)
1. **03-lineage 浅色**:展开卡头部(任务名/耗时/状态词)与元数据行(内存/带宽/演练目标)在浅色下近乎不可读——页内 `<style>` 里有硬编码深色背景/文字,需换 token 或像终端一样显式钉死深底(二选一,保持刻意感)。
2. **全局**:deck/layout 共 86 处硬编码色审一遍——状态灯语(红黄绿蓝)属刻意保留,其余(#e2e8f0/#94a3b8 这类文字色)一律换 `--text-*` token。
3. `已中止` 状态词缺席(brief 四态+已中止);补进词表(filter 或行状态均可)。
4. `.sym-bar`(共息 BPM 均衡条)与 `.stream-cursor` 的 infinite 未挂运行态类——语义上它们就是运行态指示,给外层容器挂 `.is-running` 门控即可合规(共息静默时均衡条应静止)。
5. 双主题触发:现为手动 data-theme 切换;真实现需 prefers-color-scheme 三态(system/dark/light)——原型阶段可不改,进 P2 时落。

## 明确认可(实现期保持)
- 空态设计(待机就绪+巡检清单+起步工作流)是本轮最大亮点,进 P2 原样保留语义
- 密度双模式切换、注意力收件箱严重度条+行内动作按钮、swarm 卡台账、共息 BPM 可视化、Geist/JetBrains Mono 字体栈
