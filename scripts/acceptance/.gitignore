# 验收入口的运行产物,不入库。
#
# 两个理由,第二个是硬的:
#   1. 每次跑出来的结果,不是源码,重跑即得。
#   2. 它们**必然含个人绝对路径** —— logs/*.txt 是 node --test 与子进程的原样输出,
#      acceptance-results.json 里也带 repo 路径。这不是靠「记得脱敏」能解决的,
#      所以从入库层挡掉。独立验证者(VERIFY-FINAL.md 阻塞项 3)实测这里有 7 个文件带个人路径。
#
# 需要交付证据时,把**脱敏并在文件头声明脱敏范围**的副本放进
# docs/engineering/<轮次>/logs/final/,不要直接提交这里的原始产物。
artifacts/
